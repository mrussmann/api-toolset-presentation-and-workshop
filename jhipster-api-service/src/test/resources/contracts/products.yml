description: "should return a list of products"
request:
  method: GET
  url: /api/products
  # removed so the request can run in a browser
  #headers:
  # Content-Type: application/json
  # queryParameters:
  #     limit: 1
  headers:
  #  Content-Type: application/json
response:
  status: 200
  headers:
    content-type: application/json;charset=UTF-8
  body: [
            {
                "title": "My Product One",
                "price": 10
            },
            {
                "title": "My Product Two",
                "price": 30
        }
    ]
